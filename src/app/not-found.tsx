import { NotFoundBody } from '@/components/folio/NotFoundBody';

export const metadata = {
  title: '404 — page introuvable · Page not found · Xtincell',
};

export default function NotFound() {
  return <NotFoundBody />;
}
